const redisHost = 'REDIS_HOST' as const;
const redisPort = 'REDIS_PORT' as const;
const redisUsername = 'REDIS_USERNAME' as const;
const redisPassword = 'REDIS_PASSWORD' as const;

export const redisVariableKeys = {
  redisHost,
  redisPort,
  redisUsername,
  redisPassword,
};
